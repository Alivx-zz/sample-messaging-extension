apply plugin: 'com.android.application'

android {
    compileSdkVersion 19
    buildToolsVersion '20.0.0'

    defaultConfig {
        applicationId 'com.pushbullet.android.extension.messaging.sample'
        minSdkVersion 16
        targetSdkVersion 19
        versionCode 1
        versionName '1.0'
    }

    buildTypes {
    }
}

dependencies {
    compile 'com.pushbullet:android-extensions:1.0.4@aar'
}
